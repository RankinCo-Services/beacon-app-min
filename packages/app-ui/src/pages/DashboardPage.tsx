/**
 * Placeholder dashboard page — confirms the app is wired up correctly.
 * Replace with your real app content.
 *
 * To check DB connectivity, set VITE_APP_API_URL in the shell's .env.local
 * and call your backend's /api/health endpoint here.
 */
export default function DashboardPage() {
  return (
    <div className="p-6 max-w-2xl">
      <h1 className="text-2xl font-bold text-gray-900 mb-2">Dashboard</h1>
      <p className="text-gray-500 text-sm">
        App is running. Replace this page with your real content.
      </p>
    </div>
  );
}
